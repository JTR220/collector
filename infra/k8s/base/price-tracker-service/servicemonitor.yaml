# Etape 15 (suite) : decouverte par le Prometheus Operator (kube-prometheus-stack,
# voir infra/argocd/apps/monitoring.yaml) du endpoint /metrics expose par le
# service (prometheus/client_golang, apps/backend/price-tracker-service/internal/metrics).
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: price-tracker-service
  labels:
    app: price-tracker-service
spec:
  selector:
    matchLabels:
      app: price-tracker-service
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
