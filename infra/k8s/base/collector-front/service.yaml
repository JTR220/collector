apiVersion: v1
kind: Service
metadata:
  name: collector-front
  labels:
    app: collector-front
spec:
  selector:
    app: collector-front
  ports:
    - name: http
      port: 80
      targetPort: http
