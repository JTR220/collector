# Résumé du 08/07/2026

## Sécurité
- **fix(auth)**: correction d'une injection SQL potentielle sur `GetUserInternal`, ajout de tests de couverture associés (commit `7ed43d6`).

## Qualité / lint
- **style(front)**: correction du formatage Prettier sur 4 fichiers pour faire passer le lint CI (commit `652fd07`).

## CI/CD
- **ci(backend)**: suppression du job SonarCloud du workflow `backend.yml` — un seul scan avait du sens pour tout le backend (deux scans avec le même `projectKey` s'écrasaient mutuellement) et il échouait sur les PR Dependabot (pas de secrets → "Project not found"). Le reste de la CI (lint, tests, gosec) suffit.
- **ci(front)**: ajout de la couverture de tests côté frontend
  - nouvelle dépendance `@vitest/coverage-v8`
  - script `npm run coverage` dans `package.json`
  - configuration du provider `v8` dans `vite.config.ts` (reporters `text`/`lcov`, dossier `./coverage`, exclusions des fichiers de test/config)
  - `/coverage` ajouté au `.gitignore`

## Données de démo
- **feat(seed)**: comptes vendeur/acheteur de démo liés à de vrais articles (commit `a5e0bc6`), déployé en staging.

## Déploiements
- Backend `a5e0bc6` déployé en staging (commit `1380d79`).
